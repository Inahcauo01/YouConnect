{{-- Stop trying to control. --}}
<div>
    {{-- @foreach ($notifications as $notification)
        <div>{{ $notification->data['user_id'] }} liked your post #{{ $notification->data['post_id'] }}</div>
    @endforeach --}}
</div>
    
